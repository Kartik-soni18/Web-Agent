[project]
name = "web-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiofiles>=25.1.0",
    "dotenv>=0.9.9",
    "playwright",
    "requests>=2.34.2",
]
